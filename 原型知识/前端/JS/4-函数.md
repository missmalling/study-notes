函数
函数是一段可重复使用的代码块,它接受输入(参数)、执行特定任务,并返回输出。
function function_name(参数1,参数2,参数3,…){//参数可以不写,表示不传参
	// 函数体,执行这里的代码
	return 返回值;//可选,返回值
}

作用域：
作用域分为全局作用域和局部作用域，
在函数内部声明的变量具有局部作用域也就是函数作用域，
在函数外部声明的变量具有全局作用域。